{
  "name": "allbridge-core-sdk-examples",
  "private": true,
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pnpmi": "rm -rf node_modules && pnpm i"
  },
  "dependencies": {
    "@allbridge/bridge-core-sdk": "file:../"
  },
  "devDependencies": {
    "bs58": "^5.0.0"
  }
}
